name: Deploy to Multi-Tier AWS Architecture

on:
  push:
    branches: [ "deploy" ] 

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Logueo nativo de Docker en los servidores de GitHub
      - name: Login to Docker Hub (Native)
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # Compilación en la nube de GitHub (Usa las IPs dinámicas de tus secretos)
      - name: Build and Push Images to Docker Hub
        run: |
          # 1. Compilar y subir Backend Ventas
          docker build -t oretnad8/innovatech-backend-ventas:latest ./back-Ventas_SpringBoot/Springboot-API-REST
          docker push oretnad8/innovatech-backend-ventas:latest
          
          # 2. Compilar y subir Backend Despachos
          docker build -t oretnad8/innovatech-backend-despacho:latest ./back-Despachos_SpringBoot/Springboot-API-REST-DESPACHO
          docker push oretnad8/innovatech-backend-despacho:latest
          
          # 3. Compilar Frontend inyectando AUTOMÁTICAMENTE la IP del Backend en GitHub
          docker build -t oretnad8/innovatech-frontend:latest --build-arg VITE_API_VENTAS=http://${{ secrets.AWS_HOST_BACK }}:8081 --build-arg VITE_API_DESPACHOS=http://${{ secrets.AWS_HOST_BACK }}:8080 ./front_despacho
          docker push oretnad8/innovatech-frontend:latest

      # Configurar el cliente SSH nativo del Runner de GitHub
      - name: Setup SSH Native Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.AWS_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo -e "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      # ==========================================================
      # DESPLIEGUE AUTOMÁTICO POR SSH HACIA AWS
      # ==========================================================
      - name: Deploy Database Server
        run: |
          scp docker-compose-db.yml ec2-user@${{ secrets.AWS_HOST_DB }}:/home/ec2-user/docker-compose-db.yml
          ssh ec2-user@${{ secrets.AWS_HOST_DB }} << 'EOF'
            mkdir -p /home/ec2-user/app
            cd /home/ec2-user/app
            mv /home/ec2-user/docker-compose-db.yml ./docker-compose.yml 2>/dev/null || true
            docker-compose down || docker compose down || true
            docker-compose up -d || docker compose up -d
          EOF

      - name: Deploy Backends Server
        run: |
          scp docker-compose-backends.yml ec2-user@${{ secrets.AWS_HOST_BACK }}:/home/ec2-user/docker-compose-backends.yml
          ssh ec2-user@${{ secrets.AWS_HOST_BACK }} << 'EOF'
            mkdir -p /home/ec2-user/app
            cd /home/ec2-user/app
            mv /home/ec2-user/docker-compose-backends.yml ./docker-compose.yml 2>/dev/null || true
            docker-compose down || docker compose down || true
            docker system prune -a -f
            docker-compose pull || docker compose pull
            docker-compose up -d || docker compose up -d
          EOF

      - name: Deploy Frontend Server
        run: |
          scp docker-compose-frontend.yml ec2-user@${{ secrets.AWS_HOST_FRONT }}:/home/ec2-user/docker-compose-frontend.yml
          ssh ec2-user@${{ secrets.AWS_HOST_FRONT }} << 'EOF'
            mkdir -p /home/ec2-user/app
            cd /home/ec2-user/app
            mv /home/ec2-user/docker-compose-frontend.yml ./docker-compose.yml 2>/dev/null || true
            docker-compose down || docker compose down || true
            docker system prune -a -f
            docker-compose pull || docker compose pull
            docker-compose up -d || docker compose up -d
          EOF